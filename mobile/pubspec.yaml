name: maize_classifier
description: Offline maize seed quality classifier.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image: ^4.1.7
  image_picker: ^1.0.7
  path_provider: ^2.1.2
  tflite_flutter: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/models/maize_classifier.tflite